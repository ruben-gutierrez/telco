Requerimientos

1- Debe haber en openstack pool de direcciones publicas
2- Una clave shh (file.pem)


-----------------------------------
permisos 
1- chown -R www-data rra/ log/ files_XML/
2- chmod -R 777 rra/ log/ files_XML/
3- agregar www-data a sudoers-->www-data ALL=(ALL:ALL) NOPASSWD: ALL

-------------------------
agregar funciones al crontab
    1- poller.php
    2- solicitud_asignacion.php
    3- admin_info.php

-----------------------------
crear el usuario: telco 
usename:telco > email:telco@hotmail.com > full name: telco > pass : Telco2019
habilitarlo
en los tree perms 
la politica de arbol
desabilitarlos y actualizar
y en los permisos habilitar , testbedims, graphs y ver grafica en tiempo real. El idioma pasarlo a español
dejar tema clasico


----------------------------------------------
en caso de modificar o reinstalar openstack
1- editar los datos para obtener el token       scripts/createToken.sh

    export OS_USERNAME=admin
    export OS_PROJECT_NAME=Testbed_vIMS
    export OS_PROJECT_DOMAIN_NAME=default
    export OS_USER_DOMAIN_NAME=Default
    export OS_PASSWORD=3rootbl
    export OS_AUTH_URL=http://10.55.2.24/identity

2- Editar la direccion ip openstack   scripts/request_openstack.sh
    OS_IP_OPENSTACK=10.55.2.24
    Copiar la clave key.pem en la ruta Scripts/
    en el archivo initialization_vm.sh verificar la ruta del archivo que contiene la key.pem (Testbed_vIMS.pem) 

    
    verificar el case 
        //create_vm el valor de key_name que conincida con el nombre de clave ssh.
3- functions.php 
    linea 
    function asingIpFloatServer($idServer)
        editar la variable id_network_float

    linea 
    conectRouterNetPublic($idRouter, 'c969d12a-da48-43ea-8adc-8c4620d8a8b1'); 
    en solicitud asignacion cambiar el id de red public

    linea
    $vm_create=create_vm( $nameVm, "
    reemplazar el id de la imagen de ubuntu 14

    

4- copiar la clave ssh (.pem) a la ruta scripts/terminal_testbed

5-para crear flavors hay que darle permisos a usuario en openstack
    como admin  identity/proyectos -> miembros de proyecto en admin que esta como member se agrega admin

6-instalar cacti, curl , php y python


    _______________________________--
    errror de date.time en mysql y php
    modificar los archivos 
                /etc/php/<version>/cli/php.ini
                /etc/php/<version>/apache2/php.ini

                y para mysql

                SET GLOBAL time_zone='America/Bogota';
                SET time_zone='America/Bogota';

Directory Permission Checks

Please ensure the directory permissions below are correct before proceeding. During the install, these directories need to be owned by the Web Server user. These permission changes are required to allow the Installer to install Device Template packages which include XML and script files that will be placed in these directories. If you choose not to install the packages, there is an 'install_package.php' cli script that can be used from the command line after the install is complete.

These directories will be required to stay read writable after the install so that the Cacti remote synchronization process can update them as the Main Cacti Web Site changes

NOTE:If you are installing packages, once the packages are installed, you should change the scripts directory back to read only as this presents some exposure to the web site.
Potential permission issues

All folders are writable
Required Writable at Install Time Only
/usr/share/cacti/site/resource/snmp_queries/
Writable
/usr/share/cacti/site/resource/script_server/
Writable
/usr/share/cacti/site/resource/script_queries/
Writable
/usr/share/cacti/site/scripts/
Writable
Required Writable after Install Complete
/tmp/
Writable
/usr/share/cacti/site/log/
Writable
/usr/share/cacti/site/cache/boost/
Writable
/usr/share/cacti/site/cache/mibcache/
Writable
/usr/share/cacti/site/cache/realtime/
Writable
/usr/share/cacti/site/cache/spikekill/
-----------------------------------


apuntes

intro bien
contribuciones: especificar las contribuciones a la investigacion especificar el nuevo conocimiento, mencionar lo que nos diferencia.
        evaluacion: ventajas , la mayoria de los testbed no realizan tal cosa( basado en la usabilidad).
        lo de contribuir es carreta

marco teorico
    recomendacion, poner mas arriba sip 
    centrarce en lo novedoso,


texto marco teorico 
nuestra trabajo se ecentra en .--- ofreciendo  tal cosa --- usando tal cosa----


diseño 
proceso mediante el cual se obtienen los casos sde uso

